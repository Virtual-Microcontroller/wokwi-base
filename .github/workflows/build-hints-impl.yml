name: build-hints-impl

# Writes hints INTO the student's source as /* AI-HINT: ... */ lines and
# pushes them to their branch, so help waits in the editor after a pull.
#
# Reusable body. Student repositories carry only the trigger; this holds the
# behaviour, so a correction here reaches every repository on its next failed
# build rather than only repositories created afterwards.
#
# In a called workflow the github context is the CALLER's, so
# github.event.workflow_run.* below refers to the student's failed build, and
# actions/checkout resolves to the student's repository.
on:
  workflow_call:
    secrets:
      CLAUDE_CODE_OAUTH_TOKEN:
        required: true

permissions:
  contents: write
  actions: read

env:
  # Comment marker. Must be a whole line of its own, never inline in an
  # expression, so a malformed comment cannot break a file that compiled.
  HINT_RE: '^[[:space:]]*/\* AI-HINT:.*\*/[[:space:]]*$'
  # Assisted failures allowed per repository per calendar day (UTC).
  DAILY_CAP: 10

jobs:
  hints:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          ref: ${{ github.event.workflow_run.head_branch }}
          fetch-depth: 0

      - name: Decide whether to proceed
        id: guard
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HEAD_SHA: ${{ github.event.workflow_run.head_sha }}
        run: |
          set -euo pipefail

          # The student may already have pushed a fix while this job queued.
          # Commenting on code that no longer exists is worse than silence, and
          # it also hands them a rejected push for being quick.
          tip=$(git rev-parse HEAD)
          if [ "$tip" != "$HEAD_SHA" ]; then
            echo "branch has moved: tip=$tip failed_run=$HEAD_SHA"
            echo "proceed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # A build can fail before the compiler ever runs: a source file in
          # the wrong encoding, a toolchain install that failed, a simulator
          # request that timed out. None of those are the student's code, so
          # there is no diagnostic to explain -- the model reads the log,
          # correctly edits nothing, and the invocation is counted anyway. The
          # student loses a slot from their daily allowance for a failure that
          # was never theirs.
          #
          # The compile step's own conclusion says this outright. GitHub marks
          # it "skipped" when an earlier step failed, so a skipped compile
          # means the compiler never spoke. Only one step name is relied on,
          # and the failure mode if it is ever renamed is that the lookup
          # misses and we proceed exactly as before -- so this can cost a
          # wasted invocation, but never a lost hint.
          jq_build='[.jobs[].steps[]? | select(.name == "Build") | .conclusion] | first // "absent"'
          built=$(gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${{ github.event.workflow_run.id }}/jobs" \
                  --jq "$jq_build" 2>/dev/null || echo "absent")
          echo "compile step conclusion: $built"
          if [ "$built" = "skipped" ]; then
            echo "the build failed before compiling; nothing to teach"
            echo "proceed=false" >> "$GITHUB_OUTPUT"

            # Say so. Every other thing this system does reaches the student
            # through a commit comment, which GitHub notifies on by default;
            # a failed run does not, because Actions email is opt-in. Leaving
            # this one silent would give the weakest signal to the failure the
            # student is least able to diagnose -- and, for an encoding fault,
            # one caused by the assistant's own output.
            jq_failed='[.jobs[].steps[]? | select(.conclusion == "failure") | .name] | first // ""'
            failed=$(gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${{ github.event.workflow_run.id }}/jobs" \
                     --jq "$jq_failed" 2>/dev/null || echo "")
            echo "failing step: $failed"

            # Once per commit. A re-run of the same build must not post twice.
            jq_told="[.[] | select(.body | contains(\"ai-assist-precompile-notice\"))] | length"
            told=$(gh api "repos/${GITHUB_REPOSITORY}/commits/${HEAD_SHA}/comments" \
                   --jq "$jq_told" 2>/dev/null || echo 1)

            if [ "$told" -eq 0 ]; then
              if [ "$failed" = "Check sources are UTF-8" ]; then
                {
                  echo "### ファイルの文字コードを直してください"
                  echo
                  echo "ファイルが UTF-8 で保存されていないため、コンパイルの前に"
                  echo "ビルドが止まりました。どのファイルの何バイト目かは Actions のログに"
                  echo "出ています。"
                  echo
                  echo "VS Code なら、右下のエンコーディング表示をクリックし"
                  echo "**Save with Encoding → UTF-8** を選んで保存し直してください。"
                  echo
                  echo "コードそのものは間違っていないかもしれません。文字コードだけの問題です。"
                  echo "対象は .c や .h だけではなく、Makefile、wokwi.toml、"
                  echo "diagram.json など、管理されているテキストファイルすべてです。"
                  echo
                  echo "*A tracked file is not saved as UTF-8, so the build stopped before"
                  echo "compiling. The Actions log names the file and the byte. In VS Code:"
                  echo "click the encoding indicator, then Save with Encoding -> UTF-8.*"
                  echo
                  echo "<!-- ai-assist-precompile-notice -->"
                } > precompile.md
              else
                {
                  echo "### ビルドはコンパイル前に止まりました"
                  echo
                  echo "\`${failed}\` のステップで失敗したため、コンパイラは動いていません。"
                  echo "これはあなたのコードの問題ではなく、ツールチェーンのインストールや"
                  echo "ダウンロードなど、環境側の失敗です。"
                  echo
                  echo "Actions のページから **Re-run jobs** を試してください。"
                  echo "何度も繰り返す場合は教員に伝えてください。"
                  echo
                  echo "*The build stopped before the compiler ran, so this is not your"
                  echo "code. Try Re-run jobs; tell an instructor if it keeps happening.*"
                  echo
                  echo "<!-- ai-assist-precompile-notice -->"
                } > precompile.md
              fi
              if gh api "repos/${GITHUB_REPOSITORY}/commits/${HEAD_SHA}/comments" \
                   -F body=@precompile.md > /dev/null 2>&1; then
                echo "told the student the build stopped before compiling"
              else
                echo "::warning::could not post the pre-compile notice"
              fi
              rm -f precompile.md
            else
              echo "already told the student about this commit"
            fi
            exit 0
          fi

          # Spend limit. Only runs that actually consulted the model cost
          # anything, so a run that stopped at this guard must not consume the
          # budget. Counting runs rather than invocations made the limit far
          # tighter and less predictable than intended.
          today=$(date -u +%Y-%m-%d)
          jq_today="[.[] | select(.createdAt | startswith(\"$today\"))] | .[].databaseId"
          ids=$(gh run list --workflow build-hints.yml --limit 60 --json databaseId,createdAt --jq "$jq_today")
          # A composite action contributes two steps: "Run <action>" and
          # "Post Run <action>". Both match the action's name and both succeed,
          # so matching on the name alone counted every invocation twice and
          # DAILY_CAP: 10 allowed only five. Exclude the post step.
          jq_used='[.jobs[].steps[]? | select(.name | test("claude-code-action")) | select(.name | startswith("Post") | not) | select(.conclusion == "success")] | length'
          used=0
          for id in $ids; do
            n=$(gh api "repos/${GITHUB_REPOSITORY}/actions/runs/$id/jobs" --jq "$jq_used" 2>/dev/null || echo 0)
            used=$((used + n))
          done
          echo "model invocations today: $used of $DAILY_CAP"

          if [ "$used" -ge "$DAILY_CAP" ]; then
            echo "daily cap reached"
            echo "proceed=false" >> "$GITHUB_OUTPUT"
            # Say so once a day. Ask whether a notice already exists rather
            # than inferring it from the count: invocations can jump past the
            # cap when runs overlap, and a count-based test then never fires,
            # leaving the student in exactly the silence this is meant to end.
            jq_notice="[.[] | select(.created_at | startswith(\"$today\")) | select(.body | contains(\"ai-assist-cap-notice\"))] | length"
            told=$(gh api "repos/${GITHUB_REPOSITORY}/comments?per_page=100" --jq "$jq_notice" 2>/dev/null || echo 1)
            if [ "$told" -eq 0 ]; then
              {
                echo "### 本日の AI サポートは上限に達しました"
                echo
                echo "このリポジトリは 1 日あたりの AI サポート回数を使い切りました。"
                echo "UTC の 0 時にリセットされます。"
                echo
                echo "それまでは Actions のログを自分で読んでください。**最初の**"
                echo "エラーを見ます。後ろのエラーはその波及であることがほとんどです。"
                echo "行き詰まったら教員に相談してください。"
                echo
                echo "<!-- ai-assist-cap-notice -->"
              } > capnote.md
              if gh api "repos/${GITHUB_REPOSITORY}/commits/${HEAD_SHA}/comments" -F body=@capnote.md > /dev/null 2>&1; then
                echo "posted the cap notice"
              else
                echo "::warning::could not post the cap notice"
              fi
              cat capnote.md >> "$GITHUB_STEP_SUMMARY"
              rm -f capnote.md
            else
              echo "already notified today; staying silent"
            fi
            exit 0
          fi

          echo "proceed=true" >> "$GITHUB_OUTPUT"

      - name: Clear previous hints
        if: steps.guard.outputs.proceed == 'true'
        run: |
          set -euo pipefail
          # Old markers are removed before new ones are written, so hints from
          # earlier attempts do not stack up. The diff check below permits
          # removing marker lines, and nothing else.
          find . \( -name '*.c' -o -name '*.h' \) -print | while read -r f; do
            grep -vE "$HINT_RE" "$f" > "$f.stripped" || true
            mv "$f.stripped" "$f"
          done

      - uses: anthropics/claude-code-action@3f854a8fb5146b39d5cbf8b57f70d80810e1366f # v1
        if: steps.guard.outputs.proceed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          # Pinned deliberately. The language quality of the hints is
          # now a user-visible property of this workflow, so it must not
          # change when the action's default model moves.
          claude_args: >-
            --model claude-haiku-4-5
            --allowedTools "Bash(gh run view:*),Bash(gh run list:*),Read,Edit,Glob,Grep"
          prompt: |
            You are a lab teaching assistant for an embedded systems course.
            A student's firmware build just failed in this repository.

            Failed run id: ${{ github.event.workflow_run.id }}
            Branch: ${{ github.event.workflow_run.head_branch }}
            Repository: ${{ github.repository }}

            Read the failure with:
              gh run view ${{ github.event.workflow_run.id }} --log-failed

            THE PROJECT
            Bare-metal C for the STM32F103C8 "Blue Pill" (ARM Cortex-M3).
            startup.c is the vector table and reset handler. link.ld is the
            memory layout, flash at 0x08000000 and RAM at 0x20000000. uart.c
            drives USART1 on PA9. gpio.c drives the LED on PC13 and the LED on
            PA1, and reads the button on PA0. main.c is the entry point.
            `make` builds app.elf, and the build workflow then runs it in the
            simulator, which requires the expected string on the serial line.
            Compiler flags include -Wall -Wextra -Werror, so every warning is a
            build error.

            WHAT TO DO

            STEP 1, ALWAYS, BEFORE ANYTHING ELSE. Run this exact command:

              gh run list --workflow build.yml --limit 10 --json conclusion,headSha,displayTitle

            Count how many of the most recent runs failed in a row, and say
            that number out loud in your reasoning, like "consecutive recent
            failures: 3". Do this even when the fix looks obvious. You cannot
            judge how specific your hint should be without it, and a student on
            their fourth identical failure needs something quite different from
            one on their first.

            This step is not optional and not conditional. If you skip it you
            will hand the same hint to a student who has already ignored it
            three times.

            STEP 2. Find the FIRST real error. Later errors are usually
            consequences.

            Decide what kind of failure it is:
            - Infrastructure or CI: toolchain install failed, a download timed
              out, a step was cancelled. Nothing is being taught. Do not edit
              any file at all; make no changes and stop. A re-run fixes it.
            - The student's code: compile error, linker error, or the
              simulation not finding the expected serial output. Leave hints,
              per below.

            LANGUAGE
            Write the hint text in Japanese. The students and teachers on this
            course are Japanese speakers, and a student reads a hint at the
            moment they are stuck, which is the worst moment to ask them to
            work in a second language.

            Keep these in English, exactly as they appear:
            - anything the student must TYPE: keywords (volatile, static,
              const), type names, function names (HAL_GPIO_TogglePin),
              register names (MSTPCRB), file names (link.ld), compiler flags
              (-Werror, -nostdlib)
            - anything they will SEE VERBATIM in a compiler or linker message,
              including the quoted message itself

            Everything else is Japanese, concept vocabulary included: prefer
            割り込み, ベクタテーブル, リンカ, レジスタ over their English
            equivalents. The test is simple -- if the student has to type it or
            search for it in the manual, it stays English; if it is an idea
            they have to understand, it is Japanese.

            Do not translate an identifier. A hint reading
            揮発性を指定してください is worse than useless, because the
            student then has to work out that the word to type is volatile.

            Use ASCII spaces for indentation. Never a full-width space (U+3000).

            Examples of the intended result:

              /* AI-HINT: 割り込みハンドラと共有する変数なので volatile が必要です */
              /* AI-HINT: C の文はどの記号で終わりますか。142 行目を見てください */
              /* AI-HINT: `undefined reference` はリンカが関数本体を見つけられないという意味です。SRCS にこのファイルは入っていますか */

            HOW TO LEAVE A HINT
            Use the Edit tool to insert a line directly ABOVE the line that
            needs attention, in the .c or .h file concerned. The line must
            look exactly like this, on a line of its own:

              /* AI-HINT: ヒント本文 */

            The AI-HINT: prefix is ASCII and never changes. Only the text after
            it is Japanese: the workflow recognises its own work by that prefix.

            Rules that are enforced mechanically after you finish. If you break
            any of them, nothing is pushed and the student gets no help at all:
            - You may ONLY add whole lines matching that marker exactly.
            - You may NOT change, reformat or delete any existing line.
            - You may ONLY touch .c and .h files. Not the Makefile, not
              link.ld, not any workflow file.
            - Keep the total to at most four markers. One is usually right.
            - Match the indentation of the line below it.
            - Never let the text contain the sequence that would close a
              comment early.

            GIVE HINTS, NOT SOLUTIONS. This rule matters most, and it is
            easier to break now that you are writing next to the code.
            - Never write the corrected code. No fixed lines, no diffs, no
              "change X to Y", no code fragment containing the answer.
            - A marker reading "missing semicolon" is the answer, not a hint,
              and it teaches nothing. Ask what terminates a C statement.
            - Name the concept and point at the place. Quote the exact
              compiler message and translate it into plain language.
            - Prefer one guiding question over a lecture. Keep each marker to
              a single line that a student can read at a glance. Japanese is
              compact; two short sentences usually fit where one English
              sentence did.

            ESCALATE. Use the number you counted in step 1. The same
            error failing again means your previous hint did not land, so
            repeating it is worthless -- change what you say each time:

              1 failure    name the concept, ask the question
              2 failures   say it is the same line as last time, and narrow it
                           to a specific part of that line
              3 failures   point at the exact character or token, still as a
                           question
              4 or more    stop hinting. Say plainly, in the marker, that this
                           has not worked after several tries and that they
                           should ask an instructor or TA to look at the line
                           with them.

            At four or more do NOT also give another hint. Handing over is the
            help at that point.

            COMMON HERE: -Werror promoting an unused variable; `undefined
            reference` from a file missing in SRCS; `region FLASH overflowed`;
            libc calls under -nostdlib; a build that compiles but produces no
            serial output, most often a peripheral left unclocked because its
            enable bit in RCC_APB2ENR was never set; and a missing `volatile`
            on a memory-mapped register, which produces no diagnostic at all.

            You do not run git. You do not commit, push, or open anything. You
            edit files and stop. The workflow handles the rest.

      - name: Normalise marker indentation
        if: steps.guard.outputs.proceed == 'true'
        run: |
          set -euo pipefail

          # Now that hints are Japanese, the model can indent a marker with a
          # full-width space (U+3000). [[:space:]] in the gate below is
          # ASCII-only, so such a line is not recognised as a marker and the
          # whole push is refused: a daily-cap slot spent, a red run, and no
          # help for the student -- over a formatting slip they did not make.
          #
          # Rewriting the leading whitespace of a line that is otherwise
          # exactly a marker cannot touch the student's code and cannot change
          # what the hint says. A full-width space outside a comment is not
          # valid C anyway, so this also stops the file failing to compile.
          python3 - <<'PY'
          import pathlib, re

          # [^\S\r\n] is "whitespace but not a line break", and Python's \s is
          # Unicode-aware, so U+3000 is included.
          MARKER = re.compile(r'^([^\S\r\n]*)(/\* AI-HINT:.*\*/[^\S\r\n]*)$')
          files = 0
          for path in list(pathlib.Path('.').rglob('*.c')) + \
                      list(pathlib.Path('.').rglob('*.h')):
              text = path.read_text(encoding='utf8')
              lines, touched = text.split('\n'), False
              for i, line in enumerate(lines):
                  m = MARKER.match(line)
                  if m and any(c not in ' \t' for c in m.group(1)):
                      lines[i] = ' ' * len(m.group(1)) + m.group(2)
                      touched = True
              if touched:
                  path.write_text('\n'.join(lines), encoding='utf8')
                  files += 1
                  print(f'::warning::normalised marker indentation in {path}')
          print(f'files normalised: {files}')
          PY

      - name: Check the diff before anything is pushed
        id: check
        if: steps.guard.outputs.proceed == 'true'
        run: |
          set -euo pipefail

          if [ -z "$(git status --porcelain)" ]; then
            echo "assistant made no changes"
            echo "push=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          bad=0

          # Only C sources may be touched.
          if git diff --name-only --ignore-space-at-eol | grep -vE '\.(c|h)$' > /dev/null; then
            echo "::error::a file other than .c or .h was modified"
            git diff --name-only --ignore-space-at-eol | grep -vE '\.(c|h)$' | sed 's/^/    /'
            bad=1
          fi

          # Every added or removed line must be a marker line. This is what
          # makes "comments only" a control rather than a request.
          # --ignore-space-at-eol: the model sometimes strips trailing
          # whitespace from a line it is inserting above, and refusing the
          # whole change over that means the student gets no hint and a red
          # workflow they did not cause. Trailing whitespace cannot change C
          # semantics, so it is the one difference tolerated here. Every other
          # edit to a non-marker line is still rejected.
          git diff -U0 --ignore-space-at-eol | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' \
            > /tmp/changed.txt || true

          # A marker can match the pattern and still be invalid C. The regex
          # is greedy, so `.*\*/` matches through to the LAST */ on the line
          # while the compiler stops at the FIRST. A hint whose text contains
          # */ therefore closes its own comment early and leaves the remainder
          # as bare code, so the student's next build fails with a syntax error
          # they did not write and cannot interpret. An opening /* is rejected
          # for the same reason: GCC warns on it, and -Werror makes that fatal.
          #
          # The prompt forbids both, and a real run produced one anyway. That
          # is the argument for checking rather than asking.
          while IFS= read -r line; do
            [ -z "$line" ] && continue
            body=${line:1}
            # A whitespace-only added or removed line. The raw diff line for
            # an added blank line is "+", which is not empty, so the check
            # above does not catch it and the marker test below then rejects
            # the entire push -- costing a daily-cap slot and delivering no
            # hint at all. Whitespace cannot change C semantics, which is the
            # same reason --ignore-space-at-eol is used above, so it is
            # tolerated here rather than treated as an edit to the code.
            if [ -z "$(printf '%s' "$body" | tr -d '[:space:]')" ]; then
              continue
            fi
            if ! printf '%s\n' "$body" | grep -qE "$HINT_RE"; then
              echo "::error::non-marker line in diff: $line"
              bad=1
              continue
            fi
            inner=${body#*AI-HINT:}
            inner=${inner%\*/*}
            case "$inner" in
              *'*/'*|*'/*'*)
                echo "::error::marker text would close its own comment: $body"
                bad=1
                ;;
            esac
          done < /tmp/changed.txt

          added=$(grep -c '^+' /tmp/changed.txt || true)
          if [ "$added" -gt 4 ]; then
            echo "::error::$added markers added, at most 4 allowed"
            bad=1
          fi

          if [ "$bad" -ne 0 ]; then
            echo "refusing to push; restoring the tree"
            git checkout -- .
            exit 1
          fi

          echo "diff is markers only: $added added"
          if [ "$added" -gt 0 ]; then
            echo "push=true" >> "$GITHUB_OUTPUT"
          else
            echo "push=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Push the hints
        if: steps.check.outputs.push == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          BRANCH: ${{ github.event.workflow_run.head_branch }}
          RUN_ID: ${{ github.event.workflow_run.id }}
          HEAD_SHA: ${{ github.event.workflow_run.head_sha }}
          SERVER_URL: ${{ github.server_url }}
        run: |
          set -euo pipefail
          # The branch may have moved while the agent was thinking. The guard
          # cannot catch that: it ran before the agent did. Without this check
          # git push fails as a non-fast-forward, and the student is shown a
          # red workflow they did not cause and cannot act on.
          # ls-remote asks the server. `git fetch` updates FETCH_HEAD but does
          # not reliably update refs/remotes/origin/<branch>, so rev-parse can
          # return a stale value that matches HEAD_SHA and waves the push
          # through -- which is exactly how this check failed the first time it
          # was needed, letting a push be rejected as a non-fast-forward.
          remote_tip=$(git ls-remote origin "refs/heads/${BRANCH}" | cut -f1)
          if [ "$remote_tip" != "$HEAD_SHA" ]; then
            echo "branch moved while the assistant was working; discarding"
            echo "  triggering commit: $HEAD_SHA"
            echo "  current tip:       $remote_tip"
            exit 0
          fi

          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          # [skip ci] stops GitHub building this commit. A push made with
          # GITHUB_TOKEN also cannot trigger a workflow run, so the loop is
          # closed twice over; the marker is kept because it is visible to a
          # human reading the log, and because a PAT would not have that
          # second protection.
          git commit -m "Add build hints for run ${RUN_ID} [skip ci]"
          git push origin "HEAD:refs/heads/${BRANCH}"

          # Tell the student the hint exists. Nothing else does: GitHub emails
          # the author when an issue is opened on their repository but not when
          # a branch is pushed to, so moving help from an issue into a commit
          # removed the only automatic signal they had. A commit comment does
          # notify, and is posted by the workflow rather than the model, so it
          # costs nothing.
          #
          # The pointer, never the hint itself. Repeating the text here would
          # put help back in a browser tab, which is the thing this replaced.
          # The link points at the commit just pushed, which is the one
          # carrying the hint -- not HEAD_SHA, which is the build that failed.
          hint_sha=$(git rev-parse HEAD)
          {
            echo "### ヒントが届いています"
            echo
            echo "失敗したビルドを読んで、問題のある行のすぐ上に"
            echo "コメントを入れました。"
            echo
            echo "\`git pull\` を実行して、エラーの近くにある"
            echo "\`AI-HINT\` で始まる行を探してください。"
            echo
            echo "[このコミットを見る](${SERVER_URL}/${GITHUB_REPOSITORY}/commit/${hint_sha})"
            echo
            echo "これは答えではなくヒントです。修正を自分で考えることが"
            echo "演習です。マーカーはビルドが成功した時点で自動的に削除されます。"
          } > hintnote.md
          if gh api "repos/${GITHUB_REPOSITORY}/commits/${HEAD_SHA}/comments" -F body=@hintnote.md > /dev/null 2>&1; then
            echo "told the student a hint is waiting"
          else
            echo "::warning::could not post the hint notice"
          fi
          rm -f hintnote.md
